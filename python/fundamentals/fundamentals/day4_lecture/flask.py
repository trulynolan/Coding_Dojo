# Allows you to build a back end. Submit requests. 
# Make requests to the server ->FLASK<-
# Flask is the server that allows us to communicate with the database.
# Sets up virtual environments. Virtual desktops. They to the same thing, but they are different environments.
# Will be installing a package that will allow us to do this.
# All of our virtual environments will be the same. 
# We will be creating a virtual environment with every project. 