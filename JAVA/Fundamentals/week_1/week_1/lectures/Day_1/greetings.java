public class Greeting {
    // JS
    // var name = nick;

    // default values
    int num; 
    double dubNum;
    boolean isBool;
    String name; 

    int num1 = 55; 
    double dubTwo = 5.5;
    boolean inBool2 = true;
    String name = "nick";

    // Do not use key words for variable names
    // I.E. char, class, int

    // Use camel case for variables -> thisName, nameOfPerson
    // Do not begin with numbers or whitespacwe

    // Data Types - Primitive vs Object(Wrapper Classes)

    int num3 = 4
    String name1 = "mikey";
    "Janice" = new String;

    // Conditionals - If ~ Else Statements
    if(num3 == 4) {
        System.out.println("Hey it is equal!")
        // what to do if the condition is true
    }
    else if (num3 != 4) {
        System.out.println("Hey it's not equal!")
        // what to do is 2nd condition is true
    }
    // ...can have 0 to many else-if statements...
    else{
        System.out.println("If it no exists, must be null!")
        // what to do it none of the previous conditions are met
    // } can have 1 or no else statement

    // Operators 
    int count = 1 
    // Sets count to be 1
    count == 1;
    // True
    count == 2 ;
    // False
    count != 1;
    // False
    